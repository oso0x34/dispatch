{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main Dispatch window",
  "windows": ["main"],
  "permissions": ["core:default", "opener:allow-open-path", "notification:default"]
}
